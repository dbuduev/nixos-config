keybind = ctrl+shift+]=next_tab
keybind = ctrl+shift+[=previous_tab

keybind = super+shift+]=next_tab
keybind = super+shift+[=previous_tab

# Dump terminal contents to a temp file and paste its path to the clipboard.
keybind = ctrl+shift+y=write_screen_file:paste

# title bar / decorations
# Fully borderless — PaperWM manages window framing, so no GTK chrome.
# (GNOME/Mutter has no server-side decorations, so `none` is truly borderless.)
window-decoration = none
# Toggle decorations back on at runtime if ever needed.
keybind = ctrl+shift+d=toggle_window_decorations
gtk-titlebar-hide-when-maximized = true
gtk-tabs-location = hidden
keybind = ctrl+shift+b=toggle_tab_overview

# Breathing room between terminal text and the window edge (default 2).
# Horizontal bumped to 5 now that windows sit flush to the screen.
window-padding-x = 5
